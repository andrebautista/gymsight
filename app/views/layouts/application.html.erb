<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Gymsight" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
     <div class="row">
      <div class="small-12 large-12 medium-12 large-centered columns">
        <%= render 'layouts/header' %>
      </div>
    </div>
    <div class="row <%= 'hidden' if current_page?(root_path)%>">
      <div class="small-12 large-12 medium-12 large-centered columns">
        <div class="banner-container">
          <%= image_tag @banner, alt: "#{@banner_text}", style: "width:100%;" %>
        </div>
      </div>
    </div>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>






